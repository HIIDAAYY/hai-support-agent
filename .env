# Database Configuration (Local PostgreSQL via Docker)
# This is loaded by Prisma CLI for migrations
# Using port 5433 to avoid conflict with local PostgreSQL on 5432
DATABASE_URL=postgresql://dev:devpassword@127.0.0.1:5433/urbanstyle_cs